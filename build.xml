<project name="HealthProtocol" default="build">
  <!-- Generated by MPS -->
  <property name="build.dir" location="build" />
  <property name="build.tmp" location="${build.dir}/tmp/HealthProtocol" />
  <property name="build.layout" location="${build.dir}/artifacts/HealthProtocol" />
  <property name="environment" value="env" />
  <property name="env.JAVA_HOME" value="${java.home}/.." />
  <property name="jdk.home" value="${env.JAVA_HOME}" />
  
  <target name="assemble" depends="classes">
    <mkdir dir="${build.layout}" />
    <jar destfile="${build.layout}/healthprotocolUK.jar" duplicate="preserve">
      <fileset dir="${basedir}/solutions/HealthProtocol.build/" />
    </jar>
  </target>
  
  <target name="buildDependents" />
  
  <target name="fetchDependencies" />
  
  <target name="build" depends="assemble" />
  
  <target name="clean">
    <delete dir="${build.tmp}" />
    <delete dir="${build.layout}" />
  </target>
  
  <target name="compileJava" depends="java.compile.main" />
  
  <target name="processResources" />
  
  <target name="classes" depends="compileJava, processResources" />
  
  <target name="test" depends="classes" />
  
  <target name="check" depends="test" />
  
  <target name="java.compile.main">
    <mkdir dir="${basedir}/languages/HealthProtocolUK/source_gen" />
    <mkdir dir="${build.tmp}/java/out/main" />
    <javac destdir="${build.tmp}/java/out/main" fork="true" encoding="utf8" includeantruntime="false" debug="true">
      <compilerarg value="-Xlint:none" />
      <src>
        <path location="${basedir}/languages/HealthProtocolUK/source_gen" />
      </src>
      <classpath />
    </javac>
  </target>
</project>